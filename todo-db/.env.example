# Copy this file to .env and choose a strong password. Do not commit .env.
POSTGRES_DB=todo
POSTGRES_USER=todo_app
POSTGRES_PASSWORD=change-this-to-a-strong-local-password

# Host port. Containers in the same Compose network use port 5432 instead.
POSTGRES_HOST_PORT=5432
